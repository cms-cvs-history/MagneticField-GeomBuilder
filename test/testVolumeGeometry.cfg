process MAGNETICFIELDTEST = {
#    source = EmptySource {untracked int32 maxEvents = 1}
    untracked PSet maxEvents = {untracked int32 input = 3}	
    source = DummySource { }

    service = MessageLogger {
	untracked vstring destinations = { "cout" }

	untracked vstring debugModules = { "testMagneticField" }

        untracked vstring categories     = { "RecoMuonDetLayers"}

	untracked PSet cout = {
		untracked string threshold = "DEBUG"
	 	untracked bool noLineBreaks = true 
	        untracked PSet DEBUG = {untracked int32 limit = 0 }
		untracked PSet RecoMuonDetLayers = {untracked int32 limit = 10000000}
	}
   }

    
    include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
#    replace VolumeBasedMagneticFieldESProducer.debugBuilder = true

    module testMagneticField = testMagneticField {}
    
    module testVolumeGeometry =  MagGeometryAnalyzer {}
    
    path p = {testMagneticField, testVolumeGeometry}

}
